<div class="container p-4">
    <div class="row">
        <div class="col-md-10 mx-auto">
            <div class="card">
                <div class="card-header">
                    <h3>Edit {{posts.post_type}}</h3>
                </div>
                <div class="card-body">
                    <div class="form-group">
                        <center><img src="{{posts.image_url}}" alt="IMG" width="300" height="auto"></center>
                    </div>
                    <form method="POST" action="/post/edit/{{posts.id}}" enctype="multipart/form-data">
                        <div class="form-group">
                            <input type="text" placeholder="Title" value="{{posts.title}}" class="form-control" name="title" autofocus>
                        </div>
                        <div class="form-group">
                            <textarea type="body" placeholder="Body" name="body" rows="3" class="form-control">{{posts.body}}</textarea>
                        </div>
                        <div class="form-group">
                            <label for="exampleFormControlSelect1">Select the type:</label>
                            <select class="form-control" id="post_t" name= "post_t" value="{{posts.post_type}}">
                                <option value="{{posts.post_type}}"> - </option>
                                <option>POST</option>
                                <option>EVENT</option>
                            </select>
                        </div>
                        <div class="form-group"> 
                            <input type="file" name="tg_file">
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block">
                                Save
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>